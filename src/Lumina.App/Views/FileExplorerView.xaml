<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Lumina.App.Views.FileExplorerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Lumina.App.Controls"
    xmlns:viewModels="using:Lumina.App.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="ExplorerNavigationButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="28" />
            <Setter Property="Height" Value="28" />
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="Root"
                            Background="{TemplateBinding Background}"
                            CornerRadius="{TemplateBinding CornerRadius}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames
                                                Storyboard.TargetName="Root"
                                                Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame
                                                    KeyTime="0"
                                                    Value="{ThemeResource SubtleFillColorSecondaryBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames
                                                Storyboard.TargetName="Root"
                                                Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame
                                                    KeyTime="0"
                                                    Value="{ThemeResource SubtleFillColorTertiaryBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <Storyboard>
                                            <DoubleAnimation
                                                Storyboard.TargetName="ContentPresenter"
                                                Storyboard.TargetProperty="Opacity"
                                                To="0.38"
                                                Duration="0" />
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                            <ContentPresenter
                                x:Name="ContentPresenter"
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style
            x:Key="ExplorerCommandIconButtonStyle"
            BasedOn="{StaticResource ExplorerNavigationButtonStyle}"
            TargetType="Button">
            <Setter Property="Width" Value="40" />
            <Setter Property="Height" Value="40" />
        </Style>

        <Style
            x:Key="ExplorerCommandTextButtonStyle"
            BasedOn="{StaticResource ExplorerNavigationButtonStyle}"
            TargetType="Button">
            <Setter Property="Width" Value="Auto" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Padding" Value="10,0" />
        </Style>

        <Style x:Key="ExplorerCommandContainerStyle" TargetType="AppBarElementContainer">
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="InlineRenameTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="20" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Padding" Value="2,0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="0" />
            <Setter Property="Background" Value="{ThemeResource TextControlBackground}" />
            <Setter Property="BorderBrush" Value="{ThemeResource TextFillColorPrimaryBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="UseSystemFocusVisuals" Value="False" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border
                MinHeight="54"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                BorderThickness="0,0,0,1"
                Padding="18,8,16,8">
                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition MinWidth="220" Width="*" />
                        <ColumnDefinition MinWidth="220" Width="320" />
                    </Grid.ColumnDefinitions>

                    <Button
                        Click="BackButton_Click"
                        IsEnabled="{Binding CanNavigateBack}"
                        Style="{StaticResource ExplorerNavigationButtonStyle}"
                        ToolTipService.ToolTip="Back">
                        <FontIcon
                            FontFamily="Segoe Fluent Icons"
                            FontSize="14"
                            Glyph="&#xE72B;" />
                    </Button>

                    <Button
                        Grid.Column="1"
                        Click="ForwardButton_Click"
                        IsEnabled="{Binding CanNavigateForward}"
                        Style="{StaticResource ExplorerNavigationButtonStyle}"
                        ToolTipService.ToolTip="Forward">
                        <FontIcon
                            FontFamily="Segoe Fluent Icons"
                            FontSize="14"
                            Glyph="&#xE72A;" />
                    </Button>

                    <Button
                        Grid.Column="2"
                        Click="UpButton_Click"
                        IsEnabled="{Binding CanNavigateToParent}"
                        Style="{StaticResource ExplorerNavigationButtonStyle}"
                        ToolTipService.ToolTip="Up one level">
                        <FontIcon
                            FontFamily="Segoe Fluent Icons"
                            FontSize="14"
                            Glyph="&#xE74A;" />
                    </Button>

                    <Button
                        Grid.Column="3"
                        Click="RefreshButton_Click"
                        IsEnabled="{Binding CanRefresh}"
                        Style="{StaticResource ExplorerNavigationButtonStyle}"
                        ToolTipService.ToolTip="Refresh">
                        <FontIcon
                            FontFamily="Segoe Fluent Icons"
                            FontSize="14"
                            Glyph="&#xE72C;" />
                    </Button>

                    <Border
                        Grid.Column="4"
                        Height="36"
                        Padding="12,0"
                        Background="{ThemeResource ControlFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="6">
                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <FontIcon
                                FontFamily="Segoe Fluent Icons"
                                FontSize="15"
                                Glyph="&#xE8B7;"
                                VerticalAlignment="Center" />

                            <BreadcrumbBar
                                x:Name="AddressBreadcrumbBar"
                                Grid.Column="1"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center"
                                ItemClicked="AddressBreadcrumbBar_ItemClicked"
                                ItemsSource="{Binding BreadcrumbItems}">
                                <BreadcrumbBar.ItemTemplate>
                                    <DataTemplate x:DataType="viewModels:FileExplorerBreadcrumbItemViewModel">
                                        <BreadcrumbBarItem
                                            Content="{Binding}"
                                            AutomationProperties.Name="{Binding Name}">
                                            <BreadcrumbBarItem.ContentTemplate>
                                                <DataTemplate x:DataType="viewModels:FileExplorerBreadcrumbItemViewModel">
                                                    <TextBlock Text="{Binding Name}" />
                                                </DataTemplate>
                                            </BreadcrumbBarItem.ContentTemplate>
                                        </BreadcrumbBarItem>
                                    </DataTemplate>
                                </BreadcrumbBar.ItemTemplate>
                            </BreadcrumbBar>
                        </Grid>
                    </Border>

                    <AutoSuggestBox
                        x:Name="SearchBox"
                        Grid.Column="5"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        IsEnabled="{Binding CanSearch}"
                        KeyboardAcceleratorPlacementMode="Hidden"
                        PlaceholderText="{Binding SearchPlaceholderText}"
                        QueryIcon="Find"
                        QuerySubmitted="SearchBox_QuerySubmitted"
                        Text="{Binding SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        TextChanged="SearchBox_TextChanged" />
                </Grid>
            </Border>

            <Border
                Grid.Row="1"
                MinHeight="50"
                Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                BorderThickness="0,0,0,1">
                <CommandBar
                    Padding="8,0,8,0"
                    HorizontalAlignment="Left"
                    Background="Transparent"
                    DefaultLabelPosition="Collapsed"
                    IsDynamicOverflowEnabled="False"
                    IsOpen="False">
                    <CommandBar.PrimaryCommands>
                        <AppBarElementContainer Style="{StaticResource ExplorerCommandContainerStyle}">
                            <Button
                                AutomationProperties.Name="New"
                                IsEnabled="False"
                                Style="{StaticResource ExplorerCommandTextButtonStyle}"
                                ToolTipService.ToolTip="New">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon
                                        FontFamily="Segoe Fluent Icons"
                                        FontSize="14"
                                        Glyph="&#xE710;" />
                                    <TextBlock VerticalAlignment="Center" Text="New" />
                                </StackPanel>
                            </Button>
                        </AppBarElementContainer>

                        <AppBarSeparator />

                        <AppBarElementContainer
                            Width="40"
                            Style="{StaticResource ExplorerCommandContainerStyle}">
                            <Button
                                AutomationProperties.Name="Cut"
                                Click="CutButton_Click"
                                IsEnabled="{Binding CanUseSelectedFileCommands}"
                                Style="{StaticResource ExplorerCommandIconButtonStyle}"
                                ToolTipService.ToolTip="Cut">
                                <FontIcon
                                    FontFamily="Segoe Fluent Icons"
                                    FontSize="14"
                                    Glyph="&#xE8C6;" />
                            </Button>
                        </AppBarElementContainer>

                        <AppBarElementContainer
                            Width="40"
                            Style="{StaticResource ExplorerCommandContainerStyle}">
                            <Button
                                AutomationProperties.Name="Copy"
                                Click="CopyButton_Click"
                                IsEnabled="{Binding CanUseSelectedFileCommands}"
                                Style="{StaticResource ExplorerCommandIconButtonStyle}"
                                ToolTipService.ToolTip="Copy">
                                <FontIcon
                                    FontFamily="Segoe Fluent Icons"
                                    FontSize="14"
                                    Glyph="&#xE8C8;" />
                            </Button>
                        </AppBarElementContainer>

                        <AppBarElementContainer
                            Width="40"
                            Style="{StaticResource ExplorerCommandContainerStyle}">
                            <Button
                                AutomationProperties.Name="Paste"
                                Click="PasteButton_Click"
                                IsEnabled="{Binding CanUseFolderCommands}"
                                Style="{StaticResource ExplorerCommandIconButtonStyle}"
                                ToolTipService.ToolTip="Paste">
                                <FontIcon
                                    FontFamily="Segoe Fluent Icons"
                                    FontSize="14"
                                    Glyph="&#xE77F;" />
                            </Button>
                        </AppBarElementContainer>

                        <AppBarElementContainer
                            Width="40"
                            Style="{StaticResource ExplorerCommandContainerStyle}">
                            <Button
                                AutomationProperties.Name="Rename"
                                Click="RenameButton_Click"
                                IsEnabled="{Binding CanUseSelectedFileCommands}"
                                Style="{StaticResource ExplorerCommandIconButtonStyle}"
                                ToolTipService.ToolTip="Rename">
                                <FontIcon
                                    FontFamily="Segoe Fluent Icons"
                                    FontSize="14"
                                    Glyph="&#xE8AC;" />
                            </Button>
                        </AppBarElementContainer>

                        <AppBarElementContainer
                            Width="40"
                            Style="{StaticResource ExplorerCommandContainerStyle}">
                            <Button
                                AutomationProperties.Name="Delete"
                                Click="DeleteButton_Click"
                                IsEnabled="{Binding CanUseSelectedFileCommands}"
                                Style="{StaticResource ExplorerCommandIconButtonStyle}"
                                ToolTipService.ToolTip="Delete">
                                <FontIcon
                                    FontFamily="Segoe Fluent Icons"
                                    FontSize="14"
                                    Glyph="&#xE74D;" />
                            </Button>
                        </AppBarElementContainer>

                        <AppBarSeparator />

                        <AppBarElementContainer Style="{StaticResource ExplorerCommandContainerStyle}">
                            <Button
                                AutomationProperties.Name="Sort"
                                IsEnabled="False"
                                Style="{StaticResource ExplorerCommandTextButtonStyle}"
                                ToolTipService.ToolTip="Sort">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon
                                        FontFamily="Segoe Fluent Icons"
                                        FontSize="14"
                                        Glyph="&#xE8CB;" />
                                    <TextBlock VerticalAlignment="Center" Text="Sort" />
                                </StackPanel>
                            </Button>
                        </AppBarElementContainer>
                    </CommandBar.PrimaryCommands>
                </CommandBar>
            </Border>
        </Grid>

        <InfoBar
            Grid.Row="1"
            Margin="24,16,24,0"
            IsOpen="{Binding HasError}"
            Message="{Binding ErrorMessage}"
            Severity="Error"
            Title="Folder error"
            Visibility="{Binding ErrorVisibility}" />

        <Grid Grid.Row="2" Margin="24,16,24,24">
            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="12"
                Visibility="{Binding NoLocationVisibility}">
                <FontIcon
                    FontSize="32"
                    Glyph="&#xE8B7;" />
                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="18"
                    FontWeight="SemiBold"
                    Text="Select a location" />
                <TextBlock
                    HorizontalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Choose a managed folder from the Locations pane." />
            </StackPanel>

            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="12"
                Visibility="{Binding BusyVisibility}">
                <ProgressRing
                    Width="36"
                    Height="36"
                    IsActive="{Binding IsBusy}" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Loading folder..." />
            </StackPanel>

            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="12"
                Visibility="{Binding EmptyStateVisibility}">
                <FontIcon
                    FontSize="32"
                    Glyph="&#xE8B7;" />
                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="18"
                    FontWeight="SemiBold"
                    Text="This folder is empty" />
                <TextBlock
                    HorizontalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Files and folders will appear here." />
            </StackPanel>

            <ScrollViewer
                x:Name="FileGridScrollViewer"
                HorizontalScrollBarVisibility="Disabled"
                HorizontalScrollMode="Disabled"
                IsTabStop="True"
                PointerPressed="FileGridScrollViewer_PointerPressed"
                PointerWheelChanged="FileGridScrollViewer_PointerWheelChanged"
                PreviewKeyDown="FileGridScrollViewer_PreviewKeyDown"
                UseSystemFocusVisuals="False"
                VerticalScrollMode="Enabled"
                VerticalScrollBarVisibility="Auto"
                Visibility="{Binding FileGridVisibility}">
                <ItemsControl
                    x:Name="FilesItemsControl"
                    HorizontalAlignment="Stretch"
                    ItemsSource="{Binding Files}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <controls:ExplorerGridPanel
                                ItemWidth="{Binding CardWidth}"
                                MinColumnSpacing="16"
                                RowSpacing="18" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                x:Name="FileCard"
                                Width="{Binding CardWidth}"
                                Height="{Binding CardHeight}"
                                AutomationProperties.Name="{Binding Name}"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                DoubleTapped="FileCard_DoubleTapped"
                                PointerPressed="FileCard_PointerPressed"
                                ToolTipService.ToolTip="{Binding Path=Path}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="48" />
                                    </Grid.RowDefinitions>

                                    <Grid
                                        Margin="0"
                                        Background="{ThemeResource LayerFillColorDefaultBrush}">
                                        <Image
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch"
                                            IsHitTestVisible="False"
                                            Source="{Binding ThumbnailSource}"
                                            Stretch="Uniform"
                                            Visibility="{Binding ThumbnailVisibility}" />

                                        <FontIcon
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            FontFamily="Segoe Fluent Icons"
                                            FontSize="{Binding ThumbnailIconFontSize}"
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Glyph="{Binding IconGlyph}"
                                            Visibility="{Binding IconVisibility}" />

                                        <ItemsControl
                                            Margin="8"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Top"
                                            ItemsSource="{Binding Tags}"
                                            Visibility="{Binding TagVisibility}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <controls:WrapPanel />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border
                                                        MaxWidth="116"
                                                        Margin="0,0,4,4"
                                                        Padding="7,3"
                                                        Background="{ThemeResource AccentFillColorDefaultBrush}"
                                                        CornerRadius="4">
                                                        <TextBlock
                                                            FontSize="11"
                                                            Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                                                            Text="{Binding}"
                                                            TextTrimming="CharacterEllipsis" />
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Grid>

                                    <Grid
                                        Grid.Row="1"
                                        Padding="10,6,10,7"
                                        RowSpacing="1">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <TextBlock
                                            FontSize="12"
                                            FontWeight="SemiBold"
                                            MaxLines="1"
                                            Text="{Binding Name}"
                                            TextTrimming="CharacterEllipsis"
                                            TextWrapping="NoWrap"
                                            Visibility="{Binding DisplayInfoVisibility}" />

                                        <Grid
                                            Grid.Row="1"
                                            ColumnSpacing="8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock
                                                FontSize="11"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                MaxLines="1"
                                                Text="{Binding SizeText}"
                                                TextTrimming="CharacterEllipsis" />

                                            <TextBlock
                                                Grid.Column="1"
                                                HorizontalAlignment="Right"
                                                FontSize="11"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                MaxLines="1"
                                                Text="{Binding ModifiedText}"
                                                TextTrimming="CharacterEllipsis" />
                                        </Grid>

                                        <TextBox
                                            x:Name="InlineRenameBox"
                                            Margin="-3,-2,-3,0"
                                            VerticalAlignment="Top"
                                            AcceptsReturn="False"
                                            KeyDown="InlineRenameBox_KeyDown"
                                            Loaded="InlineRenameBox_Loaded"
                                            LostFocus="InlineRenameBox_LostFocus"
                                            Style="{StaticResource InlineRenameTextBoxStyle}"
                                            Text="{Binding RenameText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Visibility="{Binding RenameEditorVisibility}" />
                                    </Grid>

                                    <Border
                                        Grid.RowSpan="2"
                                        Background="Transparent"
                                        BorderBrush="{ThemeResource TextFillColorSecondaryBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        IsHitTestVisible="False"
                                        Visibility="{Binding FocusVisibility}" />

                                    <Border
                                        Grid.RowSpan="2"
                                        Background="Transparent"
                                        BorderBrush="{ThemeResource AccentFillColorDefaultBrush}"
                                        BorderThickness="2"
                                        CornerRadius="6"
                                        IsHitTestVisible="False"
                                        Visibility="{Binding SelectionVisibility}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
